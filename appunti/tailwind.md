# Flex
il tag flex serve per dire a tailwind di organizzare gli ogggetti all'interno 
- flex-row  flex-row-reverse  row-reverse  flex-col flex-col-reverse
- flex-1 to allow a flex item to grow and shrink as needed, ignoring its initial size:
- flex-none to prevent a flex item from growing or shrinking:

- Use gap-<number> utilities to change the gap between both rows and columns in grid and flexbox layouts:
 

-  [justify main axes, content other ]justify-start justify-end justify-center justify-between justify-around justify-evenly

